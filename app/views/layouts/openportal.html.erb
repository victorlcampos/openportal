<!DOCTYPE html>
<html>
  <head>
    <title>OpenPortal</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto:regular,bold,italic,thin,light,bolditalic,black,medium&amp;lang=en" rel="stylesheet" type="text/css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">
    <%= stylesheet_link_tag    'openportal', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'openportal', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div id='left-menu'>
      <div id='logo'>
        <%= link_to image_tag((Setting[:open_portal].preferences[:logo_url].blank? ? 'logo.png' : Setting[:open_portal].preferences[:logo_url]), style: 'max-height: 65px' ), home_path %>
      </div>

      <% if user_signed_in? %>
        <div id='user-menu'><%= render 'open_portal/user_menu' %></div>
      <% end %>

      <% if user_signed_in? %>
        <div id='account-menu'><%= render 'open_portal/account_menu' %></div>
      <% end %>
    </div>

    <div id='content' class='round'>
      <div id="main">
        <%= yield %>
      </div>
    </div>

    <div id='footer' class='round'>
    </div>
  </body>
</html>
