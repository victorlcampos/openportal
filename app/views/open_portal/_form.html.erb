<% config = {
  form_attributes: form_attributes,
  exclude_form_attributes: exclude_form_attributes,
  form_attributes_config: form_attributes_config,
  exclude_associations: exclude_associations,
  form_url: namespace_url | [resource],
  form_config: form_config
} %>

<%= render 'widgets/form', form: Resource::ResourceForm.new(resource, 1, config) %>
